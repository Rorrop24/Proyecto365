<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.8</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.Pedidos360</groupId>
	<artifactId>Pedidos360</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<!-- 1. ESTO LO CONVIERTE EN EL PADRE DE TODOS -->
	<packaging>pom</packaging>
	<name>Ecosistema-Pagos</name>
	<description>Proyecto Padre para Microservicios</description>

	<!-- 2. AQUÍ CONECTAMOS TUS 6 MICROSERVICIOS -->
	<modules>
		<module>api-pedidos</module>
		<module>api-tarjetas</module>
		<module>api-pagos</module>
		<module>api-seguridad</module>
		<module>api-boletas</module>
		<module>api-historial</module>
	</modules>

	<properties>
		<java.version>21</java.version>
	</properties>

	<!-- 3. DEPENDENCIAS COMUNES LIMPIAS SIN DUPLICADOS -->
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<!-- OJO: Se borra el build plugin de Spring Boot del padre porque este pom no genera un .jar -->
</project>